import React, { Component } from "react";

export default class Home extends Component {
  render() {
    return (
      <div>
        <div>anyone can see this screen</div>
        <div>only logged in people can see it</div>
      </div>
    );
  }
}
